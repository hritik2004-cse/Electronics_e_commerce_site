import React from 'react';
import { bestProduct } from '../../data/bestproducts';

const Bestproduct = () => {
    return (
        <div>
            {bestProduct.map((items) => {
                
            })}
        </div>
    )
}

export default Bestproduct
